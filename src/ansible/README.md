## What does it do?
- Install all the packages I use depending on which distro I'm using: Fedora, Ubuntu or Void Linux.
- Clone this repository and link files to the right place if using Void

## Notes
- `roles/packages/install_*.yml`: install packages existent on distros
- `roles/packages/{Fedora,ubuntu}`: install packages adding them to the repository manually


