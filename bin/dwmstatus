#!/bin/bash

clock() {
    echo $(date +"%H:%M %p ")
}

while true
do
    xsetroot -name " $(clock)                                                                                                      $(amixer -M | head -5 | grep -o -m 1 -E "[[:digit:]]+%")  "
    sleep 1
done

